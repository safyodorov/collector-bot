---
phase: 04-media-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/services/summarizer.ts
  - src/services/obsidian.ts
  - src/services/webdav.ts
autonomous: true
requirements: [MP-04, MP-05, MP-06]
must_haves:
  truths:
    - "User receives a structured Russian-language summary of any transcribed video"
    - "An Obsidian-compatible markdown note with frontmatter is generated for each processed video"
    - "Generated notes and transcripts are uploaded to Yandex.Disk when credentials are configured"
  artifacts:
    - path: "src/services/summarizer.ts"
      provides: "LLM summarization via OpenAI-compatible API"
      exports: ["generateSummary"]
    - path: "src/services/obsidian.ts"
      provides: "Obsidian note + transcript generation"
      exports: ["createObsidianNote", "ObsidianNoteResult"]
    - path: "src/services/webdav.ts"
      provides: "Yandex.Disk WebDAV upload"
      exports: ["uploadToYandexDisk"]
  key_links:
    - from: "src/services/summarizer.ts"
      to: "openai npm package"
      via: "new OpenAI constructor with baseURL"
      pattern: "new OpenAI"
    - from: "src/services/webdav.ts"
      to: "webdav.yandex.ru"
      via: "HTTPS PUT with Basic auth"
      pattern: "webdav.yandex.ru"
---

<objective>
Create summarizer, Obsidian note generator, and Yandex.Disk WebDAV upload services.

Purpose: Process transcripts into summaries and Obsidian notes, upload to cloud storage.
Output: src/services/summarizer.ts, src/services/obsidian.ts, src/services/webdav.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-media-pipeline/04-RESEARCH.md
@.planning/phases/04-media-pipeline/04-01-SUMMARY.md

<interfaces>
From src/config.ts (created by Plan 01):
```typescript
export const OPENAI_API_KEY: string
export const OPENAI_BASE_URL: string  // default: 'https://litellm.tokengate.ru/v1'
export const SUMMARY_MODEL: string    // default: 'openai/gpt-5.2'
export const OBSIDIAN_VAULT_PATH: string // default: '/Obsidian/Media'
export const YANDEX_WEBDAV_LOGIN: string
export const YANDEX_WEBDAV_PASSWORD: string
export const TEMP_DIR: string
```

From media-transcriber-bot/summarizer.py:
- SUMMARY_SYSTEM_PROMPT: Russian prompt for structured summary generation
- SUMMARY_USER_PROMPT: Instructions for headings, key points, detailed summary, conclusion
- generate_summary(text) via OpenAI SDK with base_url, model, max_completion_tokens=16384, temperature=0.3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create summarizer service</name>
  <files>src/services/summarizer.ts</files>
  <read_first>/Users/macmini/media-transcriber-bot/summarizer.py, src/config.ts</read_first>
  <action>
First run `npm install openai` in the project directory.

Create `src/services/summarizer.ts` porting the reference bot's summarizer.py:

1. **Imports:** `OpenAI` (default import) from `openai`. Import `OPENAI_API_KEY`, `OPENAI_BASE_URL`, `SUMMARY_MODEL` from `../config.js`. Import `logger` from `../logger.js`.

2. **Prompts** (exact copy from reference summarizer.py):
```typescript
const SUMMARY_SYSTEM_PROMPT = `Ты -- профессиональный редактор и контент-аналитик. Твоя задача -- составлять \
структурированные и информативные саммари на основе транскрипций видео или аудио. \
Никогда не задавай вопросов, не проси уточнений и не добавляй комментариев от себя. \
Всегда отвечай только готовым саммари, строго следуя инструкции.`

const SUMMARY_USER_PROMPT = `Составь подробное саммари по следующей транскрипции.

Структура саммари:
1. ЗАГОЛОВОК -- краткое название темы (заглавными буквами).
2. КЛЮЧЕВЫЕ ТЕЗИСЫ -- 3-5 главных мыслей или выводов.
3. ПОДРОБНОЕ САММАРИ -- связный текст, покрывающий все основные темы, \
аргументы и примеры из транскрипции. Объём -- примерно 10-20% от длины оригинала. \
Разбивай на разделы с подзаголовками (заглавными буквами).
4. ЗАКЛЮЧЕНИЕ -- итог или основной вывод.

Форматирование:
- Выделяй заголовки и подзаголовки ЗАГЛАВНЫМИ БУКВАМИ.
- Не используй markdown-разметку: никаких **, ##, *, _ и подобных символов.
- Используй только чистый текст, нумерованные и ненумерованные списки (с тире).
- Разделяй блоки пустыми строками для читаемости.

Стиль:
- Деловой, информативный.
- Без воды и вводных фраз вроде "в данном видео рассматривается...".
- Сохраняй ключевые термины и имена из оригинала.`
```

IMPORTANT: The actual prompt text MUST use proper em-dashes, not double hyphens. Copy the exact Russian text from the reference file at /Users/macmini/media-transcriber-bot/summarizer.py. The code block above uses double hyphens only to avoid PLAN.md formatting issues.

3. **generateSummary function:**
```typescript
export async function generateSummary(transcriptionText: string): Promise<string> {
  if (!OPENAI_API_KEY) {
    throw new Error('OPENAI_API_KEY is not set')
  }

  const client = new OpenAI({
    apiKey: OPENAI_API_KEY,
    baseURL: OPENAI_BASE_URL,
  })

  logger.info({ model: SUMMARY_MODEL, textLen: transcriptionText.length }, 'Generating summary')

  const response = await client.chat.completions.create({
    model: SUMMARY_MODEL,
    max_completion_tokens: 16384,
    temperature: 0.3,
    messages: [
      { role: 'system', content: SUMMARY_SYSTEM_PROMPT },
      { role: 'user', content: SUMMARY_USER_PROMPT + '\n\n' + transcriptionText },
    ],
  })

  const summary = response.choices[0]?.message?.content ?? ''
  logger.info({ summaryLen: summary.length }, 'Summary generated')
  return summary
}
```
  </action>
  <verify>
    <automated>cd /Users/macmini/claudeclaw && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/services/summarizer.ts exists
    - grep "export async function generateSummary" src/services/summarizer.ts returns a match
    - grep "SUMMARY_SYSTEM_PROMPT" src/services/summarizer.ts returns a match
    - grep "new OpenAI" src/services/summarizer.ts returns a match
    - grep "max_completion_tokens" src/services/summarizer.ts returns a match
    - "openai" appears in package.json dependencies
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>Summarizer service compiles, uses OpenAI SDK with configurable base_url and model, Russian prompts from reference bot</done>
</task>

<task type="auto">
  <name>Task 2: Create Obsidian note generator</name>
  <files>src/services/obsidian.ts</files>
  <read_first>src/config.ts, /Users/macmini/media-transcriber-bot/handlers.py</read_first>
  <action>
Create `src/services/obsidian.ts`:

1. **Imports:** `writeFileSync`, `mkdirSync` from `node:fs`, `resolve` from `node:path`. Import `TEMP_DIR` from `../config.js`.

2. **Types:**
```typescript
export interface ObsidianNoteResult {
  notePath: string
  transcriptPath: string
  noteFilename: string
  transcriptFilename: string
}
```

3. **createObsidianNote function:**
```typescript
export function createObsidianNote(params: {
  title: string
  sourceUrl: string
  duration: number
  language: string
  summary: string
  textWithTimecodes: string
  date?: string
}): ObsidianNoteResult
```

Implementation:
- `safeTitle` = title with non-alphanumeric/space/dash replaced by `_`, truncated to 80 chars
- `date` = params.date or today YYYY-MM-DD
- Format duration as Xh Ym Zs

Main note content must be an Obsidian-compatible markdown file with YAML frontmatter containing: title, source, date, duration, language, tags (video, transcript). Then a heading with the title, a Summary section with the LLM summary text, and a Transcript section with an Obsidian wikilink to the transcript attachment file.

NOTE: The YAML frontmatter delimiters in the generated note file must be triple-dash lines. Use string concatenation to build the note content with the frontmatter block.

Transcript attachment: just `textWithTimecodes` as raw text, no frontmatter.

Write both files to TEMP_DIR. Return paths and filenames.
  </action>
  <verify>
    <automated>cd /Users/macmini/claudeclaw && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/services/obsidian.ts exists
    - grep "export function createObsidianNote" src/services/obsidian.ts returns a match
    - grep "export interface ObsidianNoteResult" src/services/obsidian.ts returns a match
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>Obsidian note generator creates proper markdown with YAML frontmatter and separate transcript file. Compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Create Yandex.Disk WebDAV upload service</name>
  <files>src/services/webdav.ts</files>
  <read_first>src/config.ts</read_first>
  <action>
Create `src/services/webdav.ts`:

1. **Imports:** `readFileSync` from `node:fs`, `request` from `node:https`. Import `YANDEX_WEBDAV_LOGIN`, `YANDEX_WEBDAV_PASSWORD`, `OBSIDIAN_VAULT_PATH` from `../config.js`. Import `logger` from `../logger.js`.

2. **uploadToYandexDisk function:**
```typescript
export async function uploadToYandexDisk(
  localPath: string,
  remotePath: string,
): Promise<void>
```

Implementation:
- Read file content with readFileSync
- Build Basic auth header from YANDEX_WEBDAV_LOGIN and YANDEX_WEBDAV_PASSWORD
- HTTPS PUT to webdav.yandex.ru at path OBSIDIAN_VAULT_PATH + '/' + remotePath
- Content-Type: text/markdown; charset=utf-8
- Resolve on 2xx status, reject otherwise
- Log success/failure

3. **ensureRemoteDir helper:**
Send MKCOL to webdav.yandex.ru at dirPath. Accept 201 (created) and 405 (already exists) as success.

4. **uploadObsidianNote convenience function:**
```typescript
export async function uploadObsidianNote(
  notePath: string,
  transcriptPath: string,
  noteFilename: string,
  transcriptFilename: string,
): Promise<void>
```
Ensures vault dir and attachments subdir exist via ensureRemoteDir, then uploads both files.
  </action>
  <verify>
    <automated>cd /Users/macmini/claudeclaw && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/services/webdav.ts exists
    - grep "export async function uploadToYandexDisk" src/services/webdav.ts returns a match
    - grep "export async function uploadObsidianNote" src/services/webdav.ts returns a match
    - grep "webdav.yandex.ru" src/services/webdav.ts returns a match
    - grep "MKCOL" src/services/webdav.ts returns a match
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>WebDAV service uploads to Yandex.Disk with directory creation. Compiles cleanly.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes
- All three service files exist with correct exports
- Summarizer uses reference bot's exact Russian prompts
- WebDAV uses Basic auth against webdav.yandex.ru
</verification>

<success_criteria>
- Summarizer generates structured summaries via OpenAI-compatible API
- Obsidian note generator produces proper markdown with frontmatter + transcript attachment
- WebDAV upload creates directories and uploads files to Yandex.Disk
- All services compile with TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/04-media-pipeline/04-02-SUMMARY.md`
</output>

---
phase: 03-cutover-deploy
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .env (on VPS)
autonomous: false
requirements:
  - CONF-05

must_haves:
  truths:
    - "Bot process 'collector' is running via PM2 on VPS"
    - "PM2 logs show no startup errors"
    - "Bot responds to a test message without errors"
  artifacts:
    - path: "/root/collector-bot/dist/index.js"
      provides: "Compiled bot entry point on VPS"
  key_links:
    - from: "PM2 process 'collector'"
      to: "/root/collector-bot/dist/index.js"
      via: "ecosystem.config.cjs"
      pattern: "script.*dist/index.js"
---

<objective>
Deploy the cleaned-up bot to VPS and verify it starts and works via PM2.

Purpose: Complete the migration by putting the Notion-free bot live.
Output: Bot running on VPS with WebDAV-only storage, processing messages end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cutover-deploy/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to VPS and verify PM2 startup</name>
  <files>.env (on VPS at /root/collector-bot/.env)</files>
  <read_first>ecosystem.config.cjs, package.json</read_first>
  <action>
1. First, commit all changes from Plan 01 and push to git (if not already pushed).

2. SSH to VPS and verify .env has WebDAV credentials and NO Notion vars:
   ```
   ssh root@212.74.231.132 'grep -c NOTION /root/collector-bot/.env; grep YANDEX_DISK_TOKEN /root/collector-bot/.env'
   ```
   - If NOTION_* vars exist in VPS .env, remove them.
   - If YANDEX_DISK_TOKEN is empty on VPS, this is a blocker — the token must be set for WebDAV to work. Check if it exists; if not, pause and ask.

3. Deploy:
   ```
   ssh root@212.74.231.132 'cd /root/collector-bot && git pull && npm install && npm run build && pm2 restart collector'
   ```
   Note: `npm install` is needed after `git pull` because @notionhq/client was removed from package.json — this cleans node_modules.

4. Check PM2 status and recent logs:
   ```
   ssh root@212.74.231.132 'pm2 list && pm2 logs collector --lines 30 --nostream'
   ```
   - Process "collector" must show status "online"
   - Logs must not contain startup errors (no "Missing required env", no "Cannot find module")
  </action>
  <verify>
    <automated>ssh root@212.74.231.132 'pm2 jlist' 2>/dev/null | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); const p=d.find(x=>x.name==='collector'); console.log(p?.pm2_env?.status==='online' ? 'PASS: collector online' : 'FAIL: collector not online')"</automated>
  </verify>
  <acceptance_criteria>
    - PM2 shows process "collector" with status "online"
    - `pm2 logs collector --lines 30 --nostream` shows no error lines containing "Missing required env" or "Cannot find module" or "NOTION"
    - VPS .env has no NOTION_* variables
  </acceptance_criteria>
  <done>Bot deployed to VPS, PM2 process running without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify bot processes a test message</name>
  <what-built>Bot deployed to VPS with WebDAV-only storage, Notion completely removed</what-built>
  <how-to-verify>
    1. Open Telegram and send a text message to the collector bot
    2. Verify the category keyboard appears (8 buttons: Бизнес, Ландшафт, ТОС, Рецепты, Семья, Новости, Идеи, Inbox)
    3. Tap "Inbox" for quick save
    4. Verify the bot responds with "Сохранено: ..." and a vault path like /Inbox/2026-03-28_...
    5. Optionally: check Yandex.Disk to confirm the .md file was created
  </how-to-verify>
  <resume-signal>Type "approved" if the bot saved a note successfully, or describe any errors</resume-signal>
</task>

</tasks>

<verification>
- PM2 process "collector" is online on VPS
- No Notion-related errors in PM2 logs
- Test message processed end-to-end: text sent -> category selected -> note saved to Yandex.Disk
</verification>

<success_criteria>
Bot runs on VPS with WebDAV only. A test message is saved to Obsidian vault on Yandex.Disk. No Notion traces remain.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cutover-deploy/03-02-SUMMARY.md`
</output>

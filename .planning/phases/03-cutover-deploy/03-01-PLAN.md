---
phase: 03-cutover-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/services/notion.ts
  - src/keyboards/hashtags.ts
  - package.json
  - .env
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04

must_haves:
  truths:
    - "config.ts exports no NOTION_TOKEN or NOTION_DATABASE_ID"
    - "config.ts exports no DEFAULT_HASHTAGS"
    - "src/services/notion.ts does not exist"
    - "src/keyboards/hashtags.ts does not exist"
    - "@notionhq/client is absent from package.json"
    - ".env has no NOTION_* variables"
    - "Bot UI texts contain no 'Notion' references"
    - "npm run build succeeds without errors"
  artifacts:
    - path: "src/config.ts"
      provides: "Clean config without Notion vars"
      contains: "YANDEX_DISK_TOKEN"
    - path: "package.json"
      provides: "Clean dependencies without @notionhq/client"
  key_links:
    - from: "src/bot.ts"
      to: "src/config.ts"
      via: "named imports"
      pattern: "import.*from.*config"
    - from: "src/bot.ts"
      to: "src/services/storage.js"
      via: "named imports"
      pattern: "import.*from.*storage"
---

<objective>
Remove all Notion traces from the codebase: dead code files, config vars, npm dependency, and .env entries.

Purpose: Complete the Notion-to-WebDAV migration by eliminating all remnants of the old storage backend.
Output: Clean codebase that builds and runs with WebDAV only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Notion dead code and config vars</name>
  <files>src/services/notion.ts, src/keyboards/hashtags.ts, src/config.ts, .env</files>
  <read_first>src/config.ts, src/bot.ts (lines 1-6 for imports), src/services/notion.ts, src/keyboards/hashtags.ts, .env</read_first>
  <action>
1. DELETE `src/services/notion.ts` entirely — it is dead code, nothing imports it (bot.ts imports from storage.js).

2. DELETE `src/keyboards/hashtags.ts` entirely — it is dead code, bot.ts imports from navigation.js.

3. EDIT `src/config.ts`:
   - Remove line: `export const NOTION_TOKEN = required('NOTION_TOKEN')`
   - Remove line: `export const NOTION_DATABASE_ID = required('NOTION_DATABASE_ID')`
   - Remove line: `export const DEFAULT_HASHTAGS = ...` (only consumer was deleted hashtags.ts)
   - Keep all other exports: BOT_TOKEN, YANDEX_DISK_TOKEN, WEBDAV_URL, VAULT_PATH, MAX_VIDEO_SIZE_MB, VIDEO_QUALITY, OWNER_CHAT_ID, ContentType, CategoryDef, CATEGORY_MAP, CATEGORY_TAGS

4. EDIT `.env`:
   - Remove the `# Notion` section header
   - Remove `NOTION_TOKEN=ntn_65908525574a...`
   - Remove `NOTION_DATABASE_ID=330f5874-4a06-8143-8924-c4587132ccc3`
   - Remove `# Хэштеги по умолчанию` section header
   - Remove `DEFAULT_HASHTAGS=...` line
   - Ensure YANDEX_DISK_TOKEN line exists (it already does, currently empty — leave as-is, VPS will have its own .env)
  </action>
  <verify>
    <automated>cd /Users/macmini/collector-bot && ! test -f src/services/notion.ts && ! test -f src/keyboards/hashtags.ts && ! grep -q 'NOTION' src/config.ts && ! grep -q 'DEFAULT_HASHTAGS' src/config.ts && ! grep -q 'NOTION' .env && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/services/notion.ts does not exist
    - src/keyboards/hashtags.ts does not exist
    - src/config.ts contains no string "NOTION_TOKEN" or "NOTION_DATABASE_ID" or "DEFAULT_HASHTAGS"
    - .env contains no line starting with "NOTION_"
  </acceptance_criteria>
  <done>All Notion references removed from config and dead code files deleted</done>
</task>

<task type="auto">
  <name>Task 2: Remove @notionhq/client dependency and verify build</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
1. Run `npm uninstall @notionhq/client` in the project root — this removes it from package.json and node_modules.

2. EDIT `package.json`:
   - Change `"description"` from `"Telegram bot that collects content into Notion database"` to `"Telegram bot that collects content into Obsidian vault via WebDAV"`

3. Run `npm run build` to verify TypeScript compilation succeeds. The build MUST succeed because:
   - bot.ts does NOT import from notion.ts (it imports from storage.js and navigation.js)
   - No remaining file imports @notionhq/client or NOTION_TOKEN

4. Verify no "Notion" string appears anywhere in src/ except possibly comments. Run: `grep -ri notion src/` — should return nothing.
  </action>
  <verify>
    <automated>cd /Users/macmini/collector-bot && ! grep -q 'notionhq' package.json && npm run build 2>&1 | tail -5 && ! grep -ri 'notion' src/ && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - package.json does not contain "@notionhq/client"
    - package.json description mentions "Obsidian vault via WebDAV" not "Notion"
    - `npm run build` exits with code 0
    - `grep -ri notion src/` returns no matches
  </acceptance_criteria>
  <done>Notion dependency removed, project builds cleanly, no Notion references in source</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `grep -ri notion src/` returns 0 matches
- `grep -q notionhq package.json` returns non-zero (not found)
- `test -f src/services/notion.ts` returns non-zero (deleted)
- `test -f src/keyboards/hashtags.ts` returns non-zero (deleted)
</verification>

<success_criteria>
All Notion traces removed. Codebase builds cleanly with WebDAV-only storage.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cutover-deploy/03-01-SUMMARY.md`
</output>
